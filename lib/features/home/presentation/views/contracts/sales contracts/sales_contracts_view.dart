// this is for view just

import 'package:flutter/widgets.dart';

class SalesContractsView extends StatelessWidget {
  const SalesContractsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
